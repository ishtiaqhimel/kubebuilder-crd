---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.11.1
  creationTimestamp: null
  name: callers.api.ishtiaq.com
spec:
  group: api.ishtiaq.com
  names:
    kind: Caller
    listKind: CallerList
    plural: callers
    singular: caller
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: Caller is the Schema for the callers API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: CallerSpec defines the desired state of Caller
            properties:
              container:
                description: ContainerSpec defines the desired state of container
                properties:
                  command:
                    items:
                      type: string
                    type: array
                  image:
                    type: string
                  restartPolicy:
                    type: string
                required:
                - command
                - image
                - restartPolicy
                type: object
              schedule:
                type: string
            required:
            - container
            - schedule
            type: object
          status:
            description: CallerStatus defines the observed state of Caller
            properties:
              completedJob:
                format: int32
                type: integer
              lastScheduleTime:
                format: date-time
                type: string
              lastSuccessfulTime:
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
